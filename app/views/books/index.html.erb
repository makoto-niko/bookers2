<%= form_with model: @user, url: books_path do |form| %>
  <div class="container px-5 px-sm-0">
   <div class="row">
   <div class="col-md-3">     
     <table>
        <tr>
          <th>User info</th>
          <td>name</td>
            <td><%= form.text_field :name %></td>
        </tr>
        <tr>
          <td>introduction</td>
        </tr>
      </table>
   <% end %>
  </div>
  
       <div class = "row">
    　 <%= link_to edit_user_path(@user), class: "btn btn-outline-secondary btn-block" do %>
         <i class="fa-solid fa-user-gear"></i>
         <% end %>
       </div>
       
     <div class="col-md-8 offset-md-1">
      <h2>Books</h2>
      <table class="table table-hover table-inverse">
      <thead>
        <tr>
          <th>Title</th>
          <th>Opinion</th>
          <th colspan="3"></th>
        </tr>
      </thead>
      <tbody>
           <% @books.each do |book| %>
      </tbody>
    </table>
    <% end %>
</div>
</div>
</div>

